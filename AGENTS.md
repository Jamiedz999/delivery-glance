## Agent skills

### Issue tracker

GitHub Issues in the `delivery-glance` repo (not yet created — see `docs/agents/issue-tracker.md`); uses the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Four label strings: `needs-triage`, `needs-info`, `ready` (covers both agent- and human-ready), `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
